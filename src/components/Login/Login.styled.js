import styled from "styled-components";
import { NavLink } from "react-router-dom";

const LoginBox = styled.div`
  background-color: #242731;
`;

const LoginTitle = styled.h1``;

const LoginUrge = styled.p``;

const LoginRedirect = styled.div``;

const LoginRedirectText = styled.span``;

const LoginRedirectLink = styled(NavLink)``;

export {
  LoginBox,
  LoginTitle,
  LoginUrge,
  LoginRedirect,
  LoginRedirectText,
  LoginRedirectLink,
};
