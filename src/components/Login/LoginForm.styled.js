import styled from "styled-components";

const LoginFormBox = styled.div`
  display: flex;
  flex-direction: column;
  width: 400px;
`;

const LoginFormInput = styled.input`
  display: block;
`;

export { LoginFormBox, LoginFormInput };
