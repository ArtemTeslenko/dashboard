import styled from "styled-components";

const SelectBox = styled.div``;

const SelectName = styled.span``;

const Select = styled.select``;

const Option = styled.option``;

export { SelectBox, SelectName, Select, Option };
