import styled from "styled-components";
import { NavLink } from "react-router-dom";

const SignupBox = styled.div`
  background-color: #242731;
`;

const SignupTitle = styled.h1``;

const SignupUrge = styled.p``;

const SignupRedirect = styled.div``;

const SignupRedirectText = styled.span``;

const SignupRedirectLink = styled(NavLink)``;

export {
  SignupBox,
  SignupTitle,
  SignupUrge,
  SignupRedirect,
  SignupRedirectText,
  SignupRedirectLink,
};
